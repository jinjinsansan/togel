# Togel 引継ぎ書（2026-08-08 第2セッション区切り）

前回の引継ぎ書（`HANDOFF_2026-08-08.md`）の続き。次のセッション（人間・AIどちらでも）は
前回分とこれを読むこと。**さらにプロジェクトルートのローカル専用ファイル（後述）が最重要**。

---

## 1. プロジェクトの現在地（結論）

**前回残っていた全作業を完了し、本番デプロイ済み。さらにコンテンツ深化の第1弾（地雷回避ガイド全24タイプ書き直し）まで実装・デプロイ済み。**

- 本番: https://www.to-gel.com （mainへのpushでVercel自動デプロイ）
- 最終コミット: `b792e72 feat: 地雷回避ガイドを全24タイプ深化（中身の正体・ラベル/タンク/警報の共通語彙）`
- typecheck / lint / next build すべてグリーン

## 2. このセッションでやったこと（コミット順）

1. `d0296ac` **OGPメタタグ接続** — シェア用LP `/share/[typeId]`（`?mode=mismatch`対応）新設。
   ミスマッチ結果のシェアURLをここに変更。layoutに`metadataBase`設定
2. `32941a5` **P3軽量適用** — LIFF診断の設問画面（ヘッダー/進捗/選択肢のみ）を新トークン化。
   adminの`#E91E63`→`primary-light`等トークン置換
3. `9202be3` **LINE週次配信基盤＋友だち追加導線** — `lib/line/broadcast.ts`（静的生成・AIコストゼロ）、
   `/api/cron/line-broadcast`（週次cron 金曜12:00 JST、CRON_SECRETフェイルクローズ）、
   `/result`・`/result/mismatch`に「週1で地雷注意報」CTA
4. `65ba233` og:imageをmetadataBase解決の相対パスに修正（デプロイ固有URL混入の防止）
5. `b792e72` **地雷回避ガイド全24タイプ深化** — 各タイプに`core`（中身の正体）新設、
   「なぜ効くか」を共通語彙で書き直し。/coaching冒頭に「地雷の仕組み（30秒でわかる版）」、
   LINE配信を5テンプレ×ワースト3=15週周期に拡張

本番動作確認済み: `/share/creative-leader?mode=mismatch` 200 / `/api/og` 200(PNG) /
`/api/cron/line-broadcast` 503（CRON_SECRET未設定によるフェイルクローズ＝設計通り）

## 3. 確定した意思決定（再議論しない）

| 決定 | 内容 |
|---|---|
| マネタイズの骨格 | 診断（バイラルフック）→ 無料の地雷回避ガイド＋LINE配信（信頼貯金）→ 有料へ。**いきなり有料商品を出さない**。有料商品の中身（C-3）は**オーナー指示で保留**——決定まで有料導線は一切入れない |
| コンテンツの理論基盤 | オーナー独自の心理学理論をベースに執筆する。**理論の名称・詳細は公開リポジトリに書かない**（ローカル専用ファイル参照）。表でも名乗らない |
| 二層構造の原則 | 仕組みの説明（ラベル・タンク・警報）は断定OK／タイプ像への当てはめは「〜が多い」で**断定禁止**（タイプはキャラクター）／個人の内面はタイプからわからないと明言する。共通締め文「**タイプは傾向、ラベルは個人。**」 |
| 共通語彙 | ラベル（思い込み）／タンク（奥にある感情）／警報（怒り=防御。全タイプ共通の機構でタイプ固有ではない）／保管距離／「なぜ？は詰問、どう？は点検」 |
| 表現ルール | 「治す」「病気」「毒親」「普通は」禁止。地雷は欠陥ではなく仕組みとして書く。診断的ラベリング禁止 |

## 4. 最重要: ローカル専用ファイル（コミット厳禁）

リポジトリは**パブリック**。以下はプロジェクトルートにあり `.gitignore` 済み。**絶対にコミットしない**。

- **`CLAUDE.md`** — コンテンツ執筆の理論知識ベース（オーナー監修による訂正2回を反映済み。
  特に「警報（怒り）のメカニズム」の節が最重要）
- **`TRANSLATION_LAYER.md`** — 翻訳レイヤー辞書／24タイプ×奥の感情マッピング表／
  パイロット清書／監修チェックリスト（C-3のみ保留、他は回答済み）

別マシンで作業する場合はこの2ファイルを手動で持っていくこと。
リポジトリをプライベート化すればコミット可能になる（オーナー判断）。

## 5. 重要ファイルマップ（今回の追加分）

- **攻略コンテンツ**: `web/src/lib/coaching/translations.ts`（全24タイプ、`core`フィールド新設済み）
- **coaching UI**: `web/src/app/coaching/page.tsx`（冒頭の共通説明＋中身の正体ブロック）
- **LINE配信文面**: `web/src/lib/line/broadcast.ts`（5テンプレ×15週周期）
- **配信cron**: `web/src/app/api/cron/line-broadcast/route.ts` + `web/vercel.json`のcrons
- **シェアLP**: `web/src/app/share/[typeId]/page.tsx`（generateMetadataでOGP接続）
- **multicast**: `web/src/lib/line/client.ts`の`multicastMessage`（500人/回上限）

## 6. 次にやること（優先順）

1. **Vercel本番に `CRON_SECRET` を設定** — 未設定のためLINE週次配信は503で無効のまま
   （ランダム値を設定するだけ。オーナーが前回env追加を保留したので必ず確認を取ること）
2. **実機確認** — /coachingの新ブロック（特にスマホ）、シェア→SNSでのOGPプレビュー、LINE内WebView
3. **C-3の決定待ち** — 有料商品（AIチャット再開／カウンセリング送客／教材）が決まったら
   coaching末尾とLINE配信への導線を設計（`TRANSLATION_LAYER.md`のチェックリスト参照）
4. 特商法の運営統括責任者名・電話は引き続き「請求時開示」方式（正式掲載はオーナー情報待ち）
5. コンテンツの個別修正 — オーナーから特定タイプの文面修正指示が来たら`translations.ts`を直接編集

## 7. 運用メモ・ハマりどころ（前回分に追加）

- **メモリ**: `togel-content-strategy` に戦略全体を記録済み（Claude Code自動メモリ）
- LINE配信の文面を変えるときは、/coachingと語彙（ラベル/タンク/警報）を必ず揃える
- コンテンツ執筆時は必ずルートの`CLAUDE.md`の理論とトーン規則に従う。
  理論解釈に迷ったら**推測せずオーナーに確認**（過去に解釈ミスを2回訂正されている）
- 有料導線は**C-3が決まるまで絶対に入れない**（オーナー明示指示）
