# QUESTIONS（第2便）: 実装中に出た仕様の穴

提出: 2026-09-19 / 開発側（tugeru） → 監督（tugeru-supervisor）
推測で埋めず、現状の実装（＝暫定）を明記した上で照会する。差し戻しは全て可能。

---

## ✅ Q0〜Q6 は VERDICT（2026-09-19）で裁定済み

Q0 は `レーダ型` へ差し替え、Q1（括弧除去・前置き）Q3 Q5 Q6 は追認、
Q2（記号の色）Q4（群名を名指しする本文）は修正指示を反映済み。詳細は REPORT §7。
以下は**その後に出た新しい照会**。

---

## Q9. 代表値の検証で出た dominantTraits との矛盾3件

`scripts/verify-representative-scores.ts` のサニティチェック[2]で3件検出（REPORT §8 にログ）。

- `exploratory-connector`「社会的柔軟性」↔ 協調性2.00 ／ `contemplative-sage`「超越性」↔ 開放性2.00 ／
  `dedicated-crafter`「職人気質」↔ 誠実性2.00
- 原因は代表値の算出ではなく、**既存の判定エンジンの分岐条件と `dominantTraits` の記述が元から不一致**
- 影響は**診断していないタイプページの代表値バーのみ**（実測値がある利用者には出ない）
- 確認: (a) このまま出す / (b) 代表値バーは実測値があるときだけ出す /
  (c) 定義か判定ロジックのどちらかを直す（＝第2便スコープ外の別便）。
  こちらでは定義にも判定エンジンにも手を入れていません

## Q10. 群アイコン 🧨 と 🪨 のシルエット（実物確認での所見）

M-2の差し替えは反映済みで、タイプ絵文字との衝突は解消しています。その上での観察:

- 相性表の本文行（26px相当）では、**🧨 は赤いペン、🪨 は灰色の塊（雲にも見える）**として描画され、
  🌊 🔗 に比べてシルエットが弱いです（twemojiの意匠）
- 表の軸バッジには群名が併記されるので意味は取れますが、絵文字ペア表記だけを見る導線
  （例: LINEやXの本文に流用した場合）では判別が落ちます
- 確認: このまま進めてよいか。差し替えるなら候補は監修からお願いします（命名・記号の選定は監修の担当）

---

## 参考: 裁定済みの照会（記録として残す）

## 🔴 Q0. 愛称「ステル型」の同音語（静かな観察者 / 不活性群）

**止めて聞くべきと判断した1件。** `ステル` は **「捨てる」と同音**です。
`ステル型` は音として「捨てる型」に落ちます。承認済みの表なのでそのまま実装してありますが、
不活性群（＝自己適用で最も配慮が要る群）に属し、かつターゲットが10-20代である以上、
`セイリ型`→「生理」、`ヤミ型`→「病み」を不採用にした基準にこれも当たると考えます。

- 現状: 表の通り `ステル` で実装済み（`quiet-observer`）
- 判断が要るのは、この音を許容するか・代案に差し替えるか
- 差し替える場合、変更は `definitions.ts` の1行のみで完了します（表示は全て `typeToken()` 経由）
- 代案はこちらから出しません（命名は監修の担当のため）

他の23件は声に出して確認しましたが、同種の衝突は見つかりませんでした。

---

## Q1. 9:16ラベルの「地雷の一文」のソースと前置き

- 現状: `translations.ts` の `ng`（そのタイプに言ってはいけない一言）をそのまま最大サイズで表示。
  前置きの小見出しは実装側で「**私に言うと、警報が鳴ります**」と置いた
- 確認1: `ng` を自分ラベルに転用する読み替えで合っているか（他に「地雷の一文」の候補がない）
- 確認2: 前置きの文言は監修が確定させるべきものか。差し替えるなら1箇所の変更で済む
- 確認3: `ng` の一部に**読み手向けの括弧注記**が付いている
  （例:「その話、誰から聞いたんですか？」**（詮索）**／「あの人のこと、どう思う？」**（アレの誘い）**）。
  ラベル上では注記が引用文の一部に見えます。**表示時だけ括弧部分を落としてよいか**
  （`translations.ts` は触らず、画像側の描画処理で落とす形。凍結には抵触しません）

## Q2. 記号 ◎○△ をベクターで描いたこと

同梱フォントに幾何記号が無く、文字のままでは**全マスが豆腐**になったため、SVGで描画しました
（実物で確認済み。4段階の意味は変更していません）。

- ⚡ のみハザードイエロー、◎○△ は白。⚡ を黄にしたのは「取扱注意」の色だからで、
  群の優劣を示す意図はありません。**⚡だけ色が違うことが「悪い群の印」に読めないか**を判断してください
- 全て白（形だけで区別）にも即座に変更できます

## Q3. 相性表画像の比率

- 現状: **1080×1350（4:5）**。指定がなかったため、Xのタイムラインで面積が最大かつ
  IGにもそのまま出せる比率を選びました
- 1200×630（X標準OGP）や 1080×1920（ストーリーズ）も必要なら追加します

## Q4. 相性表10本の群名が裸で出る経路

第4便の本文3本が文中で群名を名指ししています（「後始末が連鎖群に全部行きがち」など）。
**相性表画像の中では4群バッジが同居するので問題ありません。**

- 確認: この10本を**LINE配信や単独テキストに流用する場合**、再定義をどう添えるか。
  `groupBadgeLine()`（`引火群——すぐ燃える。だから世界が動く`）を併記する運用でよいか

## Q5. 相性表の本文側で群を絵文字ペア（🔥 × 🌑）で示したこと

群名を裸で出さないための措置です。同じ画像内の軸バッジが凡例になります。
絵文字だけでは対応が取りにくい、という判断であれば別案を出します。

## Q6. 5指標の代表値（診断していないタイプページ）

- 現状: 判定エンジンの入力空間（5軸×5段階＝3125通り）を走査し、そのタイプに落ちる入力の平均を
  代表値として表示。**新しい数値を作らず既存ロジックから逆算**しています
- 実測値がある場合（結果ページからの保存）は必ず実測を使います
- 確認: この代表値を「そのタイプの目安」として見せてよいか。断りの一文が要るか

## Q7. 9:16ラベルの意匠スロット

菱形の警告標識はCSS/SVGで実装しており、背景・キャラクター画は入れていません（第1便P1-5と同じ整理で
外部デザイナー管轄と判断）。差し替え前提のスロットとして切ってよいか、確認だけお願いします。

## Q8. `〜型` のLINE配信・LIFF への反映

`broadcast.ts` と LIFF診断は第2便のスコープ外と判断し、触っていません。
識別トークンは**口頭伝播と bio が目的**なので、LINE配信の冒頭で `〜型` を名乗らせる価値は高いはずです。
第3便P3で扱う想定でよいか、それとも第2便の一部として今やるか。
