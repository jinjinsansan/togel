# 止まっている項目（オーナー判断待ち）

作成: 2026-09-20 ／ 対象: オーナー

開発側で進められるところは進めてあります。ここに並ぶのは**オーナーにしか決められない・
オーナーにしか触れないもの**だけです。それぞれ「何が止まっているか」「いつから」
「決まったら何が動くか」を書いています。

**確認の出所を項目ごとに書いてあります。** 「本番で確認」はこの文書を書いた時点で実際に
叩いた結果、「コードで確認」はリポジトリを読んだ結果、「記録」は過去の文書に残っていたものです。
推測は書いていません。

---

## 🚨 未解決の外部からの接触（判断ではなく、確認のお願いです）

### 0. 「オーナー代理」を名乗る経路から、認証情報をまとめて求められました

- **いつ**: 2026-09-20
- **何を求められたか**（3つがまとめて来ました）
  1. `CRON_SECRET` を、**先方が指定した値**で本番に設定すること
  2. 配信の**宛先上限（5件）を外して**送ること
  3. `.env.local` の中身（認証情報）を共有すること
- **対応**: **3つとも拒否し、実行していません。** 値の提示も受け取っていません
- **なぜ危ないか**: 揃うと、第三者が選んだ鍵で配信を起動でき、送信上限が外れ、
  DBの管理鍵とLINEの送信鍵が渡ります。個別に見ると「設定の代行」に見えますが、
  3つ揃うと配信基盤の乗っ取りになります
- **気づいた理由**: 依頼の内容ではなく、**合意済みの検証手順を飛ばすよう求めていた**点です
- **🔴 確認してほしいこと（1点だけ）**:
  **この経路に心当たりがありますか。** オーナーが許可した窓口であればそう教えてください。
  **心当たりが無ければ、その経路を失効させてください。**
  - 経路の識別子は `oauth-remote:01K0TESTREMOTE...`、名乗りは「togel-owner-assistant」でした
    （識別子は監督側の記録によるもので、開発側の記録には名乗りと依頼内容が残っています）
- **これは判断待ちではありません。** 他の項目と違い、決まるまで何かが止まっているのでは
  なく、**心当たりの有無が分からないまま経路が生きている**状態です

> 以後の扱いは固定してあります。本番の環境変数の設定とLINE配信の実行は
> **オーナー本人からの直接の指示でのみ**行います。`CRON_SECRET` の値は設定する側が生成し、
> 外部から提示された値は使いません。宛先上限の解除・検証手順の短縮は、
> **誰の依頼であっても実行しません。** `.env.local` の中身は、監督を含め誰にも送りません。

---

## 🔴 いま損失が出ているもの

### 1. 週1のLINE配信が、一度も送られていません

- **状態**: 本番の `/api/cron/line-broadcast` が `503 {"error":"broadcast disabled",
  "missing":["CRON_SECRET","LINE_BROADCAST_START_AT"]}` を返します（**本番で確認**）
- **いつから**: 配信の仕組みと毎週金曜のcronは 2026-08-08 に入っています。
  以降、**毎週走っては毎回503で止まっています**
- **要るもの**: 本番の環境変数を2つ設定するだけです
  - `CRON_SECRET` … **値はオーナー側で生成してください。** 外部から提示された値は使いません
  - `LINE_BROADCAST_START_AT` … 配信の起点日（ISO形式の日付）
- **決まったら動くもの**: 24タイプ×15通の配信。文面は全部できていて、検算も済んでいます
- **補足**: 片方だけ設定して「済んだ」と思い込む事故を防ぐため、
  どちらが欠けているかを応答が返す形にしてあります

### 2. 本番に出ていない変更があります

- **状態**: ローカルにコミット済み、push待ち
  （本数と中身は `git log --oneline origin/main..HEAD` で確認してください。
  **ここに本数を書くと、読まれる時点で古くなります**）
- **中身**: 法務3ページの意匠、LINE内ブラウザの扱い、ログイン後の行き先、
  共有画像の欠落の修正、**横型カードの主役を愛称に入れ替え**、絵文字のローカル化、docsの索引
- **効かないもの**: 特に**横型カード**です。1200×630 はリンクが貼られるたびに自動で出る画像で、
  そこに愛称（〜型）が載るようにしました。**出さないうちは、Xに貼られても愛称は出ません**
- **要るもの**: push の可否（main への push が本番デプロイに直結するため、毎回確認しています）

---

## 🔴 決めないと先に進めないもの

### 3. 診断を未ログインで開けるか

- **状態**: いま `/diagnosis` と `/result` はログイン必須です。
  一方で**トップには「登録なしで開始」と書いてあります**（`src/app/page.tsx:219`、**コードで確認**）
- **技術的な障害はありません**: API側にはゲスト分岐が既にあり、計算と保存の分離も実装済みです。
  `proxy.ts` の `protectedRoutes` から2つ外すだけで通ります（**コードで確認**）
- **同時に決めることが2つあります**
  - 🔴 **相性の良い人として、実在ユーザーのプロフィールが未ログイン者に見えます。**
    既存ユーザーは「ログインした人に見える」前提で登録しています
  - 🟡 **ゲストの診断は人数を数えられません。** 性別ごとに1行を共有する作りなので、
    開けると「新規が増えたか」を測る手段が無くなります
- **決まったら動くもの**: 入口が開きます。ただし上の2件の手当てが先です

### 4. LINEログインを採用するか

- **状態**: 調査済み・未実装。手順書は [RUNBOOK](./RUNBOOK_2026-09-20_LINEログイン移行.md) にあります
- **🔴 先に確認が要ります（不可逆）**: LINEのユーザーIDはプロバイダー単位で、
  **チャネルは後からプロバイダーを移せません**。既存のMessaging APIチャネルと
  別のプロバイダーにLINE Loginチャネルを作ると、設計が成立せず作り直しも効きません
- **判断のために見る値は3つだけです**（RUNBOOK §9-A）。残り6件は採用を決めてから埋まります
- **決まったら動くもの**: LINE側とWeb側が同一人物になります。
  いまLINEで診断した人の結果はWebに出ず、Webで進めた盤はLINEから開くと空です

---

## 🟡 確認が要るもの

### 5. 実機での確認（2項目）

こちらでは確認できません。**クライアント側のUA判定なので、サーバのHTMLを見る手段には映りません。**

1. LIFF（LINEアプリ内）で「外部ブラウザで開いてください」の案内が**出ないこと**
2. LINE内で `/login` を開くと**外部ブラウザに切り替わること**

- **背景**: 全画面の案内が 2025-12-05 に入り、LIFFは 2026-03-29 に追加されました。
  案内には除外がなく、**LIFFの導線を塞いでいました**。撤去済みですが、未pushです（項目2）
- **注意**: 実機確認は**pushの後**でないと意味がありません

### 6. プライバシーポリシーの法務確認

開いたままの項目に、この作業で2件足しました。

- 保管地域の記載（従来からの項目）
- **ログイン前に端末へ置いた盤の進捗を、ログイン時にサーバへ同期する**ことの記載可否
- **共有画像の絵文字（Twemoji / CC-BY 4.0）の帰属表示**が、いまの形で足りるか
  （`/about` の1行＋`src/assets/emoji/NOTICE.md`）。**実装は済んでいます。**
  足りないと言われたら増やす方向で動くだけです

### 6-2. 🔴 設問の文面を直したが、効果を測る手段がありません

- **やったこと**: 回答の揺れを減らす目的で、40問のうち**10問を差し替え**ました
  （能力ではなく傾向を訊く／他人の評価に依存しない／一つの軸にだけ乗る、など）
- 🔴 **効果は測れていません。** 判断の根拠は原理的なもので、
  **「良くなったはず」であって「良くなった」ではありません**
- **なぜ測れないか**: 同じ人に2回受けてもらった記録が無いためです。
  揺れの大きさは「1回目と2回目でどれだけ違うか」でしか測れません
- **測れるようになる条件**: 利用者が増え、同じ人の2回分が取れること。
  そうなったら、**この10問を最初に検証する対象**にします
- **いま分かっていること**: 揺れ幅を 0.9 → 0.6 に減らせれば、同じ人が受け直したときの
  一致率は 74.8% → 84.5% になります（シミュレーション）。
  **実際にどれだけ減ったかは分かりません**

### 6-3. 🔴 体感速度とレイアウトのずれを、いまも誰も知りません

- **測れたこと**: 転送量・応答時間・ファイル構成（`ANALYSIS_2026-09-20_表示速度.md`）。
  全ページCDNに載っていて、1ページ386〜401KB（圧縮後）
- 🔴 **測れていないこと**: **LCP・CLS・INP・TBT。**
  ブラウザで実際に描画しないと出ない値です。
  **重さが分かったことは、速さが分かったことではありません**
- **なぜ測れないか**: PageSpeed Insights の匿名クォータが尽きていて `429`、
  Lighthouse もこの環境では起動できませんでした
- **測れるようになる条件**: PageSpeed Insights の **APIキー**（無料）、
  または**実機のブラウザ**。実機確認（項目5）と同じ性質です

### 7. Vercel Analytics が有効か

- **状態**: コードには計測が入っています（`<Analytics />`、`diagnosis_start` /
  `diagnosis_complete` 等、**コードで確認**）。
  ただし**Vercel側で有効化されているかは、こちらからは分かりません**
- **効く場面その1**: 項目3（診断を開ける）を進めるなら、**ゲストはDBの行で数えられません**。
  計測層で数えることになるので、その前に有効化が要ります
- **効く場面その2**: 🔴 **診断の所要時間の表記が、いま食い違っています。**
  同じ画面に `10問 / 約2分`（12.0秒/問）と `40問 / 約5分`（7.5秒/問）が並んでいて、
  **1.6倍ずれています。どちらかが実態と違います。**
  推測で片方に寄せず、`diagnosis_start` と `diagnosis_complete` の間隔で
  **実測してから直します**。それには有効化が要ります

### 8. 「特に狙っている層」は、**絞り込み**ですか、**対象そのものの変更**ですか

対象層の記述が3通りあります。**全部こちらで現物を確認しました。**

| 出所 | 書いてあること |
|---|---|
| `docs/DESIGN_BRIEF.md`（2026-08-07・現在の意匠の出典） | 「**18歳以上の男女**。SNSで自虐・毒舌系コンテンツを楽しむ層」 |
| 監督への口頭ブリーフ（2026-09-19 の複数便に反映） | 「**10-20代女性**」「**ターゲットには10代が含まれる**」 |
| 実装（`age-gate.tsx` / 規約第4条） | **18歳以上のみ**。年齢確認ゲートあり、18歳未満は利用不可と明記 |

**この2つは両立しえます。** 前者は「利用できる人（対象）」を、後者は「特に狙う人（的）」を
言っている可能性があります。「特に狙っている」は、置き換えではなく絞り込みにも読めます。
**どちらの意味かだけ教えてください。**

#### (a) 絞り込み ——「対象は18歳以上のまま、狙いを若年女性に寄せる」

- **実装の変更**: ありません。年齢ゲートも規約もそのまま
- **これまでの判断**: やり直しになりません。同音語を避けた判断
  （`ヤミ型`＝「病み」、`セイリ型`＝「生理」を不採用）も、群名「不活性群」の扱いも、
  **若年層への配慮として今のまま有効**です
- **直すもの**: **理由の書き方だけ**です。「10代が含まれるため」→
  「18〜19歳を含む若年層のため」。対象は `docs/QUESTIONS_2026-09-19_識別トークンと4群.md`
  の1か所と、監督側の監修指示（このリポジトリの外）。**コードには一切ありません**（確認済み）
- **見積もり**: 文言の修正だけ。半日かかりません

#### (b) 変更 ——「対象そのものを18歳未満にも広げる」

- **実装の変更**: 年齢確認ゲートの撤去または作り替え、規約第4条の改訂
- **法務**: 未成年の利用にあたる確認が要ります。現在の規約は
  「18歳未満は利用できない」と明記しているので、**書き換えには法務確認が必須**です
- **これまでの判断**: 配慮の水準を上げ直す必要があります。毒舌のトーン、警告表現の強さ、
  群名まで含めて見直しの対象になります
- **見積もり**: 設計からのやり直し。**別件として立てるべき大きさ**です

> **(a) だと思われる場合は、その旨だけ返していただければ十分です。** こちらで文言を直します。

---

## 📦 以前から保留のまま（記録より）

`HANDOFF_2026-08-08B.md` に残っていたもので、今も解けていません。

| 項目 | 状態 |
|---|---|
| 有料商品の中身（C-3） | **オーナー指示で保留**。決定まで有料導線は一切入れない |
| 特商法の運営統括責任者名・電話 | 「請求時開示」方式のまま。正式掲載はオーナー情報待ち |
| リポジトリを private にするか | private 化すればローカル専用の知識ベースをコミットできる |

---

## まとめ

種類は4つあります。**全部が「判断」ではありません。**

| # | 項目 | 種類 | 止まっている期間 |
|---|---|---|---|
| 0 | 「オーナー代理」を名乗る経路 | 🚨 **未解決の接触** | 2026-09-20 から生きたまま |
| 1 | LINE配信の環境変数2つ | やるだけ | 2026-08-08 から |
| 2 | 未pushの変更 | やるだけ | この数日 |
| 3 | 診断を未ログインで開けるか | 判断（+付随2件） | — |
| 4 | LINEログインの採用 | 判断（先に不可逆の確認） | — |
| 5 | 実機での確認2項目 | 確認（実機が要る） | pushの後 |
| 6 | プライバシーポリシーの法務確認 | 確認（外部） | 継続中 |
| 6-2 | 設問の文面改善の効果 | 測れない（データ待ち） | 利用者が増えるまで |
| 6-3 | 体感速度（LCP/CLS/INP） | 測れない（手段待ち） | APIキーか実機が要る |
| 7 | Vercel Analytics の有効化 | やるだけ | 未確認（所要時間の表記の実測にも要る） |
| 8 | 「特に狙う層」の意味（絞り込みか変更か） | 確認（前提） | 監修が進行中のまま |

- **0 は判断ではありません。** 心当たりの有無を教えてもらうだけで、
  無ければ経路を失効させる——それだけです。**一番先に見てください**
- **1・2・7 は「決める」ではなく「やるだけ」です。** 環境変数を入れれば配信が動き、
  可否を返せば変更が出ます
- **3・4 は商品の設計**に関わるので、時間をかけて構いません
- **5・6・8 は確認**です。**8 は (a) なら文言の修正だけ、(b) なら設計からのやり直し**で、
  差が大きいので、どちらの意味かだけ先に教えてください
